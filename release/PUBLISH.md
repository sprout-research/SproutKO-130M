# Publication targets

- Code: https://github.com/sprout-research/SproutKO-130M (new repository; do not rename or push the training repository).
- Model bundle: https://huggingface.co/project-iconik/SproutKO-130M.
- Both code and model bundle: Apache-2.0.

Create an empty public GitHub repository with the name above, then push this independent repository's main branch.
Upload only the five files in weights/ to the model Hub: model.safetensors, config.json,
sproutko-tokenizer-32k-v1.json, LICENSE, README.md (five bundle files).
The original tokenizer manifest is intentionally omitted because it is a training artifact.
Before making an existing private Hub public, review its current files and history;
this local preparation has not altered the existing Hub repository or its visibility.
